spring:
  application:
    name: api-gateway

  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      username: config-user
      password: config-pass
      fail-fast: true
      retry:
        initial-interval: 3000
        max-attempts: 10
        max-interval: 10000
        multiplier: 1.3